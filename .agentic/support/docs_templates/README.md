# System documentation templates

These are templates for **long-lived software/system documentation** (not project status).

They are used by `bash .agentic/tools/sync_docs.sh` to scaffold a `docs/` folder in a project repo.


